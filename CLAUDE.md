# CLAUDE.md

이 저장소는 **제1회 대한민국 AI창작문학상 영화 부문** 응모작(A4 3~5매 기획안 + 프롬프트 노트)을 준비하면서 창작 과정 전체를 기록하는 곳이다. 전략과 요강은 `기획전략.md`를 따른다. 접수는 2026-11-30 마감, 목표 제출일은 2026-11-25.

## 기록 규칙 (매 작업 세션)
1. `journal/_TEMPLATE_창작일지.md`를 복사해 `journal/YYYY-MM-DD_NN_제목.md`를 만든다. 같은 날 두 번째 세션이면 NN을 올린다.
2. **사용자 프롬프트는 원문 그대로** 남긴다. 방향을 바꾼 프롬프트만 골라도 된다.
3. 채택/수정/기각의 **판단과 이유는 작가(사용자)의 결정만** 적는다. AI가 판단이나 이유를 지어내지 않는다. 결정이 안 났으면 `보류`, 이유를 모르면 `-`로 둔다.
4. 결정 사항은 `journal/결정로그.md`에도 한 줄씩 추가한다.
5. 영상에 쓸 만한 순간이 있으면 일지의 "🎬 영상화 메모"와 `video/장면후보.md`에 추가한다.
6. 단계가 넘어가면 `README.md`의 진행 현황 체크박스를 갱신한다.
7. 세션 끝에 커밋하고 `main`에 push한다. 커밋 메시지 접두어: `[strategy]` `[journal]` `[idea]` `[synopsis]` `[visual]` `[video]` `[chore]`

## 파일 관리
- 시놉시스와 기획안 원고는 `synopsis/`에 `v0.1`, `v0.2`처럼 버전별로 둔다. 덮어쓰지 않는다.
- 이미지는 `assets/`에 `종류_대상_v번호` 규칙으로 저장한다 (`assets/README.md` 참고).

## 주의
- 이 저장소는 **PUBLIC**이다 (2026-09-15 기준). 로그라인이나 시놉시스 등 스토리 본문을 push하기 전에, 사용자가 공개 여부를 정했는지 확인한다.
- 문장 퇴고 시 AI 특유의 과장된 수사를 경계한다. 최종 문장은 작가가 쓴다.
